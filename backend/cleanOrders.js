const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/cctv-ecommerce').then(async () => { const db = mongoose.connection.db; await db.collection('orders').deleteMany({ orderNumber: { $in: ['SK-ORD-19823', 'SK-ORD-82665'] } }); console.log('Deleted duplicates'); process.exit(0); });
